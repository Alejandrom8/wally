module.exports = {
    host: {
        port: 8080,
        URL: 'http://192.168.1.76'
    },
    player: {
        //[0] - the length of the room id;
        //[1] - the length of the player id (it's the socket id);
        id_size: [4, 20]
    }
};